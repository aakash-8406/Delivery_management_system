# Copy to .env.local for local development with shared mock server:
# VITE_API_URL=http://localhost:3001

# For production (injected by Jenkins — never commit real values):
# VITE_API_URL=https://your-api-id.execute-api.ap-south-1.amazonaws.com/prod
# VITE_MASTER_KEY=your-master-key-here
